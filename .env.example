# OpenAI API Key (required for official OpenAI usage)
# OPENAI_API_KEY=sk-abc123def456

# Replace with your OpenAI API key. If using a local backend, set to "no-api-key-needed".
OPENAI_API_KEY=no-api-key-needed

# OpenAI Base URL
# For OpenAI's official API, use "https://api.openai.com".
# OPENAI_BASE_URL=https://api.openai.com

# For local OpenAI-compatible backends (e.g., LMStudio, Ollama), provide the appropriate URL.
OPENAI_BASE_URL=http://localhost:1234/v1

# Default OpenAI Model
# Specify the default model to use for requests (e.g., "gpt-3.5-turbo", "gpt-4").
# OPENAI_MODEL=gpt-3.5-turbo

# Local backends may have different model names (e.g., "llama2" for ollama).
OPENAI_MODEL=local-llm

# Port for local API server (default: 5000)
API_PORT=5172

# Port for local web server (default: 5173)
CLIENT_PORT=5173